/**
 * ehrax.dev Design System — public surface.
 *
 * App + primitive code should consume the semantic layer (CSS vars `--ex-*` or
 * `theme.semantic.*`) so light/dark theming works. Reach for the primitive
 * `palette` only for design-system internals and token galleries.
 */

export * from "./theme";
export * from "./tokens";
